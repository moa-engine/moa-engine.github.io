export declare class RegExpMatch {
    fullMatch: string;
    captures: string[];
    constructor(fullMatch: string, captures: string[]);
    getCapture(index: number | string, defaultValue?: string): string | undefined;
}
