export interface ProcessedSelectorInterface {
    mangledSelector: string;
    prefix: string | null;
}
export declare class MinifiedSelectorGenerator {
    private readonly letters;
    private readonly lettersLength;
    private readonly lastLetterIndex;
    processedSelectors: Record<string, ProcessedSelectorInterface>;
    getSelectorPrefix(selector: string): string;
    getStringToMatch(selector: string, addPrefix?: boolean): string;
    generateMangledSelector(selector: string, prefix?: string | null): string;
    getMangledSelector(selector: string, prefix?: string): string;
    private divideLengthAndGetLetter;
}
export declare const minifiedSelectorGenerator: MinifiedSelectorGenerator;
